# Processed Datasets

The data preparation scripts save processed datasets and record-boundary files in this directory.

The default MA experiment requires:

```text
dataset_ma_nv1.pkl
dataset_ma_nv2.pkl
flag_train.npy
train_record_names.npy
```

These generated files are not included in the repository. See the project-level `README.md` for data preparation commands.
